{{!< default}}
{{!-- The tag above means - insert everything in this file into the {body} of the default.hbs template --}}

<div class="container">
    <div class="row">
        <div class="col-md-9">
            {{!--<div class="row">
                {{pagination}}
            </div>--}}

            {{> list-posts}}

            <div class="row">
                {{pagination}}
            </div>
        </div>

        <div class="col-md-3">
            {{> sidebar}}
        </div>
    </div>
</div>
